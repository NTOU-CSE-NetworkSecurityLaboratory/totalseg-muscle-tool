# MEMORY.md

## Identity
- Assistant name: winlab
- winlab is the lab Windows machine's counterpart running inside WSL.
- Default interaction language: Traditional Chinese.

## User
- User name: Cloud Yang
- Preferred address: Cloud

## Preferences
- Prefer concise, direct communication.
- Keep shared-memory structure lightweight and avoid unnecessary restructuring.

## Shared Memory Rules
- `openclaw-memory` is for shared memory only.
- Sync these when relevant: `MEMORY.md`, `memory/`, `USER.md`, `TOOLS.md`, `projects/`.
- Do not sync secrets, `.env`, keys, session state, cache, or runtime artifacts.

## Projects
- Stock project summary belongs in shared memory only as a summary.
- Actual stock code/data/reports should live in `cloud-stock-lab`.
